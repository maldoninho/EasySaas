# Exemplos de deploy

Os arquivos são referências e devem ser adaptados ao caminho, usuário, domínio e localização do Node/pnpm da VPS. A API permanece interna em `127.0.0.1:4000`; o Caddy expõe somente a Web na porta 3000.
