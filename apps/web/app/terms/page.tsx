import Link from "next/link";import { serverApi } from "@/lib/api";export default async function Page(){const data=await serverApi<{content:{title?:string;body?:string}}>("/api/v1/public/legal/terms");return <main className="legal-page"><Link href="/">← Voltar</Link><h1>{data.content.title??"Termos de Uso"}</h1><div className="legal-content">{data.content.body??"Documento ainda não configurado."}</div></main>}
