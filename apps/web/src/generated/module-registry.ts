/* Arquivo gerado. Não editar manualmente. */
import type { ComponentType } from "react";
export type ModuleViewLoader = () => Promise<{ default: ComponentType }>;
export const moduleViewLoaders: Record<string, ModuleViewLoader> = {
};
